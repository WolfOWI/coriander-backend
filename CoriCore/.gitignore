# Project-specific files
# (Most patterns are now handled by the root .gitignore)

# Additional sensitive configuration files
appsettings.Staging.json
appsettings.*.json
!appsettings.json

# Project-specific temporary files
*.tmp.cs